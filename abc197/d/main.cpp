#include <bits/stdc++.h>
#include <atcoder/all>

using namespace std;
using namespace atcoder;

#define rep(i,n) for(int i=0;i<n;i++)
#define Rep(i,n) for(int i=1;i<=n;i++)

#define ll long long

#define P pair<int,int>

int main() {
  int n; cin >> n;
  P p0, opp, center;
  cin >> p0.first >> p0.second >> opp.first >> opp.second;
  
  return 0;
}